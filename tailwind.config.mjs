/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // Usa 'class' para habilitar el modo oscuro con una clase CSS
    content: ['./src/**/*.{astro,html,js,jsx,ts,tsx}'], // Ajusta las rutas según tu proyecto
    theme: {
      extend: {},
    },
    plugins: [],
};