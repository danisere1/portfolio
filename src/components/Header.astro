---
import meLogo from '../assets/Me.jpg';
import HeaderButton from './HeaderButton.astro';
---

<header 
class="sticky top-0 z-50 flex justify-between items-center pt-8 pb-3 w-full relative backdrop-blur bg-transparent"
>
    
    <nav class="flex flex-row gap-x-10 opacity-90 px-50 flex-wrap ml-auto">
        <HeaderButton href="">
            Experience
        </HeaderButton>
        <HeaderButton href="">
            Projects
        </HeaderButton>
        <HeaderButton href="">
            Studies
        </HeaderButton>
        <HeaderButton href="">
            Contact
        </HeaderButton>
    </nav>
    <div class="absolute left-0 right-0 -bottom-2 h-0.5 bg-white rounded-full opacity-40"></div>
</header>