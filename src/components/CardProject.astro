---
import CardTitle from './CardTitle.astro';
import { parseHighlights  } from '../lib/highlightParser';
import Highlight from './Highlight.astro';

interface Props {
  title: string;
  description: string;
  github: string | null;
  image_url: string | null;
}

const { title, description, github, image_url } = Astro.props as Props;
const parts = parseHighlights (description);
---

<a href={ github } target="_blank" class="pl-7 pr-7 pt-7 pb-7 flex flex-col items-center justify-center bg-white/3 backdrop-blur-md rounded-2xl border border-white/20 shadow-md  hover:shadow-lg scale-100 transition-all duration-700 ease-in-out w-full card-hover-effect card-hover-color">

    <div class="pb-5 gap-2 flex flex-row items-center justify-between">
        <CardTitle>
            { title }
        </CardTitle>
    </div>
    <p class="pb-3">
        {parts.map((part) =>
            typeof part === 'string' ? part : <Highlight>{part.highlight}</Highlight>
        )}
    </p>
    {image_url && image_url !== "null" && image_url !== "" && (
        <img src={ image_url } alt="Imagen del proyecto" />
    )}

</a>